# 综合物探体素建模模块说明

## 1. 功能与入口

模块提供三个相互独立、可叠加的模型：

- 视电阻率三维体素模型：Viridis 或 Jet 连续色谱、透明度与 X/Y/Z 剖切。
- 富水三维异常体：仅提取 `ρs < 570 Ω·m` 的实际输入体素，作为独立蓝色半透明图层。
- 综合围岩分级模型：按 `Vp/Vs` 得到初始等级，富水体素劣化一级，输出Ⅱ、Ⅲ、Ⅳ、Ⅴ四个独立图层。

路由为 `/geophysical-voxel`。平台内入口位于“隧洞围岩 → 超前预测 → 综合物探体素建模”。

## 2. 数据输入约束

页面初始场景为空。模型模块不调用 `fetch`、`axios` 或其他外部数据接口，也不自动读取 `public/data`。只有用户在数据对话框中粘贴的内容或明确选择的本地文件会被解析。

三类数据均支持 CSV、JSON：

- CSV 有表头：`x,y,z,resistivity`、`x,y,z,vp`、`x,y,z,vs`。
- CSV 无表头：固定按 x、y、z、标量四列解析；这兼容交接数据中有表头的 `Vp.csv` 和无表头的 `Vs.csv`。
- JSON 点对象数组：字段为 x、y、z 和相应标量。
- JSON 四元数组：每个点按 `[x, y, z, scalar]` 解析。
- JSON 根对象可用 `points`、`data` 或 `values` 承载点数组，并可提供 `voxelSize: [dx, dy, dz]`。

体素尺寸优先使用输入对象的 `voxelSize`；未提供时，从三个坐标轴的实际相邻坐标间距中取中位数。若某轴只有一个坐标且没有 `voxelSize`，构建会明确报错，不会假定厚度。

## 3. 严格过滤与空间对齐

- x、y、z 或标量不是有限数值时剔除该点。
- Vp、Vs 必须大于 0；`Vs ≤ 0` 不参与计算，避免除零或无穷大。
- 视电阻率只要求为有限数值；不会擅自改写或补齐。
- Vp 与 Vs 必须同时输入，缺少任一项时仅可构建视电阻率和富水模型。
- 分类目标网格严格使用视电阻率的实际点位。
- Vp/Vs 与目标点完全重合时直接取值；分辨率不一致时，在规则网格内执行三线性插值。
- 插值点越界或任一所需角点缺失时，该目标体素不分类；不做最近邻补点，不外推。

## 4. 分级边界

| 条件 | 初始等级 |
| --- | --- |
| `Vp/Vs < 1.7` | Ⅱ |
| `1.7 ≤ Vp/Vs < 2.0` | Ⅲ |
| `Vp/Vs ≥ 2.0` | Ⅳ |

当且仅当 `ρs < 570 Ω·m` 时劣化一级：Ⅱ→Ⅲ、Ⅲ→Ⅳ、Ⅳ→Ⅴ。`ρs = 570 Ω·m` 明确按非富水处理。

## 5. 性能策略

- 点位与属性使用 `Float32Array`，富水掩码和等级使用 `Uint8Array`。
- 显示层使用 `THREE.InstancedMesh`，不会为每个体素创建独立 Mesh。
- 全部有效体素参与阈值提取、插值和分级。
- 单图层超过渲染预算时按固定步长选择已有真实点位进行 LOD；页面同时显示“已渲染/可用”数量。LOD 不生成新点、不改变输入值。
- 重新构建和卸载页面时释放几何体、材质、控制器和 WebGL 渲染器。

## 6. 关键文件

- `src/views/GeophysicalVoxelWorkbench.vue`：输入对话框、图层控制、剖切和自检 UI。
- `src/modules/geophysical/input.ts`：CSV/JSON 解析、字段兼容与安全过滤。
- `src/modules/geophysical/model.ts`：规则网格采样、严格三线性插值、富水提取和围岩分级。
- `src/modules/geophysical/VoxelScene.ts`：InstancedMesh、色谱、透明度、相机和剖切。
- `src/modules/geophysical/types.ts`：公开数据结构。

## 7. 已完成工程自检

- 生产构建：`vue-tsc -b` 与 `vite build` 均通过。
- 真实文件验证：用交接包中的 `tem_voxel_full.csv` 通过页面文件选择器输入，解析 640,000 个有效点，严格提取 249,890 个 `ρs < 570` 的富水体素；页面未自动读取该文件。
- 数学边界验证：`1.699→Ⅱ`、`1.700→Ⅲ`、`1.999→Ⅲ`、`2.000→Ⅳ`；富水条件分别劣化为Ⅲ、Ⅳ、Ⅴ；`ρs=570` 不劣化；`Vs=0` 返回无效。
- 交互验证：视电阻率/富水图层切换、Viridis/Jet 切换、透明度和 X 轴剖切均通过浏览器验收。

本次没有把现场图片、网页报告图或波形图片转换为几何点位；这些附件仅作为业务与视觉参考。
