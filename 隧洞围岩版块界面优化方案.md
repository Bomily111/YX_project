# 隧洞围岩版块界面优化方案

## 1. 优化目标

在不删除现有任何版块、数据入口和交互功能的前提下，将“隧洞围岩”由多个浮动面板叠加的界面，调整为“左侧导航目录 + 中央三维场景 + 右侧业务面板 + 底部联动分析”的稳定工作台。

核心目标：

- 左侧采用参考 PDF 第 12-16 页的树状导航目录，建立清晰、可展开的业务层级。
- 中央继续保留 Cesium 三维场景作为主视图，不改变已有模型加载、定位、透视、测量、剖切等能力。
- 左侧通过一级、二级和必要的三级目录完成业务分流；右侧只显示当前目录节点对应的信息和操作，不再设置承载全部业务的综合页签。
- 底部增加与场景联动的里程轴、剖面图和参数趋势图，减少信息对三维场景的遮挡。
- 统一围岩“设计基准—超前预测—揭露校正”的模型演化逻辑，并补充模型参数、可信度及变化分析。

## 2. 参考页面的可借鉴结构

参考 PDF 第 12-16 页反复使用同一套页面骨架：

1. 顶部：系统名称、项目/隧洞选择、用户入口。
2. 左侧：固定一级目录，当前业务展开二级目录，选中项高亮。
3. 中央：三维模型或掌子面主场景，占据最大视觉面积。
4. 场景上方：图层、工点视角、作业面视角、施工数据漫游、量测、标注等轻量工具。
5. 右侧：当前业务的指标、结论、表格、风险或分析图。
6. 底部：工序时序、里程轴、围岩分级、参数曲线或监测结果，与中央场景联动。

本项目可以借用其信息架构，但继续保持当前深色数字孪生视觉，不必复制参考平台的白色主题。

## 3. 当前界面诊断

当前“隧洞围岩”已经具备较完整的能力：

- 顶部场景入口：隧洞围岩、开挖爆破、围岩支护、通风除尘、装备调度。
- 全局工具：里程搜索、管理后台、线路总览、复制视角、漫游、测量、剖切、截图、坐标查询、全屏。
- 图层控制：隧道中线、影像底图、隧道模型、围岩模型、通风模型、里程刻度、模型透视、地形透明度。
- 总览信息：当前掌子面、围岩等级、循环进尺、开挖方式、断面面积、当前工序、风险预警、TSP 结论。
- 围岩实时数据：近 7 天进尺、围岩稳定性、超前支护、地下水、瓦斯浓度、活跃预警。
- 围岩数字孪生：设计基准、超前预测、揭露校正三个阶段。
- 属性维度：围岩等级、完整程度、坚硬程度、地下水状态、初始地应力。
- 数据与模型：综合物探体素建模、TSP、TEM、地质雷达、掌子面照片与素描。
- 模型操作：围岩等级筛选、透明度/点大小调整、变化显示、数据依据、设计分段定位、数据预览、新建处理、历史记录。
- AI 助手及现有后台管理入口。

主要问题：

- 左侧“实时数据面板”和浮动“图层控制”会相互遮挡，导航、工具和业务数据没有明确分区。
- 顶部主导航占用横向空间，进入围岩场景后只显示“返回总览/隧洞围岩”，无法体现围岩内部层级。
- 右侧长面板同时承担阶段切换、业务选择、参数设置和历史记录，滚动深、定位成本高。
- 关键参数散落在多个卡片中，缺少统一的“当前里程—当前断面—当前模型版本”上下文。
- 三维场景、右侧结论与底部里程没有形成强联动，用户需要在多个入口之间反复确认位置。

## 4. 推荐页面骨架

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 系统标题 │ 项目/隧洞/工点选择 │ 里程搜索 │ 数据时间 │ 管理/用户             │ 64
├──────────────┬──────────────────────────────────────┬────────────────────────┤
│ 左侧阶段目录 │ 中央三维场景                         │ 右侧功能与信息面板     │
│ 220-240 px   │                                      │ 360-400 px             │
│              │ [图层] [视角] [量测] [剖切] [标注]  │ 当前里程/模型版本      │
│ 总览         │                                      │ 当前阶段相关功能组     │
│ 隧洞围岩 ▼   │                                      │                        │
│  工点概况    │                                      │                        │
│  设计基准    ├──────────────────────────────────────┤ 属性维度（5项）        │
│  超前预测    │ 底部横向轴区：默认里程轴，按版块切换 │ 超前地质预报（5项）    │
│  揭露校正    │ 围岩等级轴/参数轴/预报剖面等         │ 结论、参数和操作       │
│  监测预警    │                                      │                        │
│  数据档案    │                                      │                        │
│ 开挖爆破     │                                      │                        │
│ 围岩支护     │                                      │                        │
│ 通风除尘     │                                      │                        │
│ 装备调度     │                                      │                        │
└──────────────┴──────────────────────────────────────┴────────────────────────┘
```

布局建议：

- 顶部固定 64 px；左侧默认 232 px，可收起为 64 px 图标栏。
- 右侧默认 380-420 px，可折叠；属性维度五个版块和超前地质预报五个版块保留在右侧，以分组卡片呈现。
- 底部默认 180-220 px，可在“收起/标准/放大”三档切换，只放适合横向展开的里程轴、参数轴或预报剖面。
- 中央三维场景始终获得剩余空间，不允许业务卡片以自由浮层方式长期遮挡。
- 1440 px 以下自动收窄两侧；小屏时右侧以抽屉打开，底部图表以标签页切换。

## 5. 左侧导航目录

### 5.1 一级导航

1. 线路总览
2. 隧洞围岩
3. 开挖爆破
4. 围岩支护
5. 通风除尘
6. 装备调度
7. 系统管理

现有顶部五个场景入口整体迁移到左侧一级导航，功能和路由保持不变。顶部仅保留系统级信息和快速操作。

### 5.2 “隧洞围岩”分级目录

左侧目录采用 PDF 中的分级方式，但只承载稳定的业务阶段，不把属性维度和预报方法拆成左侧目录：

1. 工点概况
   - 当前掌子面
   - 施工状态
   - 风险摘要
2. 围岩数字孪生
   - 设计基准
   - 超前预测
   - 揭露校正
3. 掌子面揭露
   - 照片与素描
   - 岩性与结构面
   - 破碎及出水记录
4. 监测预警
   - 围岩稳定性
   - 地下水与瓦斯
   - 变形监测
   - 风险清单
5. 数据档案
   - 模型版本
   - 处理记录
   - 原始资料
   - 报告与导出

“设计基准、超前预测、揭露校正”是三个并列目录节点，负责切换围岩数字孪生的阶段状态，不放到底部区域。点击“超前预测”后，右侧显示属性维度和超前地质预报两组功能；点击其中的 TSP，中央加载 TSP 模型，底部切换为 TSP 纵向参数轴。

目录只负责“去哪”，不在左栏塞入参数控件。图层控制改放到中央场景顶部工具条的“图层”抽屉中。

## 6. 右侧功能与信息面板

右侧保留现有 PredictionCenter 的核心组织方式，承担“当前阶段下可选的属性和预报方法”。不设置“概览/参数/分析/记录”四页签，也不把十个功能卡拆到左侧。

### 6.1 固定信息头

无论选择哪个目录，右侧顶部仅保留一条紧凑的上下文信息：

- 当前隧洞、工点、桩号或预报范围。
- 当前数据时间、模型版本和数据状态。
- 面板折叠按钮及必要的返回上级操作。

### 6.2 超前预测阶段的右侧结构

右侧分成两个清晰的纵向分组：

1. 属性维度
   - 围岩等级
   - 完整程度
   - 坚硬程度
   - 地下水状态
   - 初始地应力
2. 超前地质预报
   - 综合物探体素建模
   - TSP 地震波反射
   - TEM 瞬变电磁
   - 地质雷达
   - 掌子面照片与素描

两组均使用紧凑卡片或按钮矩阵。点击卡片后，卡片下方或右侧面板下半部分显示该项的结论、阈值、图例和必要控制；大型曲线不挤在右侧，而是切换到底部横向轴区。若后续接入超前钻孔，可作为超前地质预报的新增第六项，不替换现有功能。

### 6.3 其他阶段的右侧结构

| 左侧目录节点 | 右侧主要内容 |
|---|---|
| 设计基准 | 数据依据、围岩等级图例、设计分级区段、区段定位 |
| 超前预测 | 属性维度五项、超前地质预报五项、变化显示、数据依据 |
| 揭露校正 | 掌子面揭露记录、校正结果、预测—揭露偏差、版本信息 |
| 当前掌子面 | 围岩等级、工序、循环进尺、开挖方式、断面面积、风险摘要 |
| 监测预警 | 最新值、阈值、状态、告警及处置建议 |
| 数据档案 | 模型版本、处理记录、原始资料及导出操作 |

### 6.4 面板容量规则

- 右侧上半部分放功能选择，下半部分放“结果摘要 + 必要参数 + 当前操作”。
- 大图、长曲线、长表格全部放到底部分析区，右侧只保留缩略指标和结论。
- 历史记录进入左侧“数据档案”，不与当前模型参数堆在同一面板。
- 数据处理入口随右侧预报方法切换，例如选中“TSP 地震波反射”后显示“数据预览/新建处理/历史记录”。
- 当同一节点内容仍然较多时使用可折叠分组，不再增加一层横向页签。

## 7. 底部联动分析区

底部的定位不是目录或阶段导航，而是专门承载横向较长、需要共享里程坐标的“轴”。设计基准、超前预测、揭露校正始终保留在左侧目录，不能移到底部。

默认状态显示基础里程轴，标注起止桩号、当前掌子面、预报范围、已揭露范围和风险区段。进入具有合适横向成果的版块时，底部按以下规则切换：

| 当前选择 | 底部轴/长图 | 说明 |
|---|---|---|
| 围岩等级 | 围岩等级轴 | 使用此前设计的分级色带，显示设计、预测或揭露等级及变化区段 |
| 完整程度 | 完整性参数轴 | 按里程显示完整性指数或完整/破碎分区 |
| 坚硬程度 | 坚硬程度参数轴 | 按里程显示等效 Rc 或坚硬程度分区 |
| 地下水状态 | 地下水异常轴 | 按里程显示视电阻率、富水异常和风险等级 |
| 初始地应力 | 地应力参数轴 | 按里程显示 σ1/σ3 及高地应力区段 |
| 综合物探体素 | 综合解释轴 | 叠加围岩等级、破碎带和富水异常等融合成果 |
| TSP 地震波反射 | TSP 参数轴 | 显示 Vp、Vs、Vp/Vs 及异常区间 |
| TEM 瞬变电磁 | TEM 电阻率轴 | 显示电阻率剖面、等值区及富水异常 |
| 地质雷达 | 雷达纵剖面 | 沿测线/里程展示反射异常 |
| 掌子面照片与素描 | 掌子面记录轴或基础里程轴 | 有连续揭露记录时显示记录节点，否则回退到里程轴 |
| 揭露校正 | 等级对比轴 | 数据充分时显示设计—预测—揭露三轨；否则显示基础里程轴 |
| 监测预警 | 监测趋势轴 | 数据适合按里程展示时显示分布轴，时序数据则显示对应长曲线 |
| 其他无适合轴的版块 | 基础里程轴 | 不为了填充空间强行制作无意义图表 |

所有轴共享同一里程坐标和交互游标。点击轴上的区段或数据点，中央相机定位到相应位置，右侧同步更新该里程的参数和结论。

## 8. 建议新增的模型参数与图表

### 8.1 参数卡

| 参数 | 推荐单位/表达 | 用途 | 数据状态 |
|---|---|---|---|
| 围岩等级 | Ⅱ-Ⅴ级 | 设计、预测、揭露主分类 | 已有 |
| 完整性指数 | Kv 或 0-1 | 表征岩体完整程度 | 已有模型维度，可补数值 |
| 单轴抗压强度 | Rc / MPa | 岩体坚硬程度 | 建议新增 |
| 纵波速度 | Vp / m·s⁻¹ | TSP 判识、完整性分析 | 已有数据资产 |
| 横波速度 | Vs / m·s⁻¹ | TSP 判识 | 已有数据资产 |
| 波速比 | Vp/Vs | 岩性、含水异常辅助判断 | 可计算 |
| 视电阻率 | Ω·m | TEM 富水异常判识 | 已有模型能力 |
| 初始地应力 | σ1/σ3 / MPa | 高地应力风险分析 | 已有维度，可补曲线 |
| 涌水量 | L/min | 地下水状态与风险 | 建议新增 |
| 拱顶沉降/周边收敛 | mm、mm/d | 稳定性与施工响应 | 建议接入监测数据 |
| 预测可信度 | 0-100% | 告知模型结果可靠程度 | 建议新增 |

### 8.2 图表组合

- “围岩等级里程带”：最优先，直接建立空间位置与等级变化的认知。
- “设计—预测—揭露三轨对比”：突出数字孪生持续更新的价值。
- “多参数同里程曲线”：Vp、Vs、Vp/Vs、电阻率、Kv 共享横轴。
- “多源贡献雷达图”：TSP、TEM、GPR、钻孔、掌子面揭露的权重/有效性。
- “预测可信度带”：结果值旁同时显示置信范围，避免单值造成过度确定感。
- “风险分布矩阵”：按里程和风险类型展示富水、破碎、高地应力、塌方风险。
- “断面参数图”：在掌子面断面上展示结构面、出水点、风化程度和采样位置。
- “版本差异卡”：记录本次模型相对上一版本新增、调整、撤销的区段。

所有新参数应显示数据来源和更新时间；数据尚未接入时使用“待接入/暂无数据”，不要以模拟数值冒充实测值。

## 9. 交互联动规则

| 用户操作 | 中央场景 | 右侧面板 | 底部分析区 |
|---|---|---|---|
| 点击左侧“设计基准” | 显示设计围岩分段 | 展示依据、图例、区段参数 | 显示设计等级里程带 |
| 点击左侧“超前预测” | 进入预测模型状态 | 展示属性维度五项与超前地质预报五项 | 默认显示基础里程轴 |
| 点击右侧“围岩等级” | 按围岩等级着色 | 展示等级筛选、图例与结论 | 切换为围岩等级轴 |
| 点击右侧“TSP” | 叠加 TSP 体素/异常体 | 展示 TSP 结论、参数和操作 | 切换为 TSP 参数轴 |
| 点击左侧“揭露校正” | 高亮已揭露与修正区 | 展示素描、校正结果、偏差 | 有数据时显示三轨对比，否则显示里程轴 |
| 点击里程或图表点 | 相机飞到对应位置 | 刷新该里程的参数与结论 | 同步十字游标 |
| 点击风险 | 高亮风险体或影响区间 | 展开原因和处置建议 | 定位风险里程段 |
| 调整属性/筛选 | 即时更新模型着色 | 保留当前参数状态 | 曲线和图例同步过滤 |
| 选择历史版本 | 加载指定模型版本 | 展示版本说明 | 显示版本差异 |

## 10. 现有功能迁移表（零删除）

| 当前功能/版块 | 优化后位置 |
|---|---|
| 顶部五个场景入口 | 左侧一级导航 |
| 里程搜索 | 顶部项目上下文区 |
| 线路总览、管理、复制视角 | 顶部右侧快捷区 |
| 漫游、测量、剖切、截图、坐标、全屏 | 中央场景顶部工具条 |
| 图层控制及透视/透明度 | 场景工具条“图层”抽屉 |
| 当前掌子面信息 | 左侧“工点概况 → 当前掌子面”对应的右侧任务面板 |
| 近 7 天进尺、状态详情、预警 | 左侧分级目录对应页面 + 底部监测趋势 |
| 设计基准/超前预测/揭露校正 | 左侧“围岩数字孪生”下三个并列目录节点 |
| 设计数据依据、等级图例、分级区段 | 左侧末级目录对应的右侧任务面板 + 底部里程带 |
| 五个属性维度、等级筛选、透明度、点大小 | 右侧“属性维度”分组及当前属性详情区 |
| 变化显示、数据依据 | “预测—揭露偏差”或“数据依据”目录对应页面 |
| 综合物探、TSP、TEM、GPR、掌子面素描 | 右侧“超前地质预报”五个功能卡 |
| 数据预览、新建处理、构建模型 | 右侧当前预报方法的操作区 |
| 历史记录/历史版本 | 左侧“数据档案”分级目录 |
| 底部里程进度条 | 升级为轴切换区：默认里程轴，按版块切换专业轴 |
| AI 助手 | 保持右下角全局入口，避免占用业务导航 |

## 11. 视觉规范建议

- 保留当前深蓝/青色技术风，左侧目录采用低饱和深蓝底，选中项使用青蓝渐变条。
- 统一卡片圆角 6 px、边框透明度和阴影，减少各组件各自发光造成的噪声。
- 用颜色表达状态而不是装饰：青色为当前/交互，绿色为正常，黄色为关注，橙色为预警，红色为严重。
- 关键数值使用等宽数字字体；单位降低一个字号，避免数字与单位竞争。
- 三维模型的围岩等级色和图表图例必须全平台统一，不随页面变化。
- 每个图表保留标题、单位、图例、数据时间和空状态；不使用只有颜色、没有文字的表达。

## 12. 实施优先级

### 第一阶段：只重构布局，不改业务逻辑

- 新增左侧导航目录并迁移顶部场景入口。
- 将图层控制迁移到场景工具条抽屉。
- 将实时数据、当前掌子面和 PredictionCenter 重新装入固定布局。
- 建立左侧阶段目录、右侧两组功能卡与底部轴切换规则。
- 保持所有原事件、API、模型加载和路由不变。

### 第二阶段：复用现有数据完善图表

- 设计围岩等级里程带。
- Vp、Vs、Vp/Vs、TEM 电阻率多参数曲线。
- 设计—预测—揭露三轨对比。
- 模型版本与数据来源说明。

### 第三阶段：接入新增业务数据

- Rc、Kv、地应力、涌水量、变形监测。
- 多源贡献度、预测可信度和预测准确率。
- 风险矩阵、自动判识结论和报告导出。

## 13. 验收标准

- 现有所有入口、开关、图层、模型、处理流程、历史记录和 AI 助手均可访问。
- 任一时刻中央三维场景不被两个以上长期浮动面板遮挡。
- 设计基准、超前预测、揭露校正可直接从左侧到达；五个属性和五个预报方法可直接从右侧选择。
- 当前隧洞、工点、里程、模型阶段和版本在页面中始终可见。
- 点击里程、模型区段、风险或图表数据点时，三维场景、右侧信息和底部图表同步。
- 1920×1080 下中央场景有效宽度不低于页面宽度的 52%；1440×900 下无主要功能遮挡。
- 新增图表都有单位、图例、数据时间、数据来源和无数据状态。
- 围岩等级、风险等级和状态颜色在三维、图表、列表中保持一致。

## 14. 建议的首版交付范围

首版建议先完成“左侧阶段目录 + 右侧属性/预报功能组 + 底部轴切换区”，同时加入三个最有价值、且能复用现有数据的图表：

1. 围岩等级里程带。
2. Vp/Vs/电阻率多参数曲线。
3. 设计—预测—揭露三轨对比。

这一范围可以先解决页面组织和空间冲突，又能显著提升围岩模型的专业表达，不依赖大规模新增后端数据。
