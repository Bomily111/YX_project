# 隧洞围岩设计分级功能交付与合并说明

## 1. 交付范围

本交付包包含本次“隧洞围岩”模块的全部修改内容，目标功能如下：

1. 右侧“围岩分级模型”面板增加版本选择：
   - 设计版
   - 超报版
2. 设计版依据勘察设计报告中的围岩分级资料，对当前隧洞模型按里程区段进行围岩等级着色。
3. 每个设计里程区段显示：
   - 起止里程
   - 围岩等级
   - 对应等级颜色
4. 点击右侧“设计分级区段”中的任一区段，Cesium 镜头平滑跳转到该区段的中心位置。
5. 被点击区段进入选中状态，对应隧洞模型显示白色轮廓提示。
6. 切换到超报版时，清除设计版颜色、标签和选中轮廓，恢复原隧洞模型显示，保留后续接入超前地质预报分析结果的界面入口。

除上述内容外，项目其他业务模块未作调整。

## 2. 交付文件

请按照交付包内的相对路径，将以下文件合并到负责人项目中：

| 相对路径 | 合并方式 | 作用 |
| --- | --- | --- |
| `src/views/Overview/PredictionCenter.vue` | 对照合并或替换 | 右侧设计版/超报版面板、图例、设计分级区段列表、点击与键盘交互、选中样式 |
| `src/views/index.vue` | 建议对照合并 | 接收版本切换与区段点击事件，连接页面和 Cesium 围岩模型控制函数 |
| `src/utils/Common/DrawLine.ts` | 建议对照合并 | 加载设计分级数据、模型分级着色、标签生成、恢复显示、镜头跳转和区段轮廓高亮 |
| `public/data/tunnel/design-rock-grades.json` | 新增文件 | 设计版围岩里程区段、等级、颜色及模型序号映射 |
| `reference/勘察设计报告纵断面图.png` | 参考资料，不参与编译 | 本次设计分级数据的原始参考图 |

注意：如果负责人项目中的上述 `.vue` 或 `.ts` 文件已有其他人并行修改，请不要直接整文件覆盖，应按照第 4 节中的功能点对照合并。

## 3. 数据文件说明

数据地址：

`/data/tunnel/design-rock-grades.json`

每个区段字段含义：

| 字段 | 含义 |
| --- | --- |
| `id` | 区段唯一标识 |
| `startMileage` | 起始里程显示文本 |
| `endMileage` | 终止里程显示文本 |
| `startChainage` | 起始数值里程，单位 m |
| `endChainage` | 终止数值里程，单位 m |
| `grade` | 围岩等级，当前使用 `II`、`III`、`IV`、`V` |
| `color` | Cesium 模型显示颜色，十六进制 |
| `modelIndex` | 对应当前隧洞 GLB 分段模型的序号，从 0 开始 |

当前等级颜色：

| 围岩等级 | 颜色 |
| --- | --- |
| Ⅱ级 | `#18f59a` |
| Ⅲ级 | `#4dc9ff` |
| Ⅳ级 | `#ffc928` |
| Ⅴ级 | `#ff5364` |

当前项目隧洞模型由 17 个 GLB 区段组成，因此设计数据也按 17 个模型区段进行映射。原纵断面截图文字分辨率有限，本次数据按当前模型范围 `YK1+116～YK9+400` 进行了对应归一化。后续如取得高清勘察报告原图或正式围岩分级表，只需调整 JSON 中的里程、等级和 `modelIndex` 映射，无需修改页面或 Cesium 控制逻辑。

## 4. 代码合并重点

### 4.1 `PredictionCenter.vue`

主要内容：

- 新增设计版、超报版页签。
- 设计版展示数据依据、建模流程、围岩等级图例和 17 个设计分级区段。
- 超报版展示超前地质预报数据处理流程及待接入状态。
- 新增事件：
  - `versionChange`
  - `segmentSelect`
- 点击区段时发送完整 `DesignRockGradeSegment` 数据。
- 区段行增加 `role="button"`、`tabindex="0"` 和 `Enter` 键操作。
- 使用 `selectedDesignSegmentIndex` 管理当前选中区段。
- 切换版本时清除区段选中状态。

### 4.2 `index.vue`

主要内容：

- 在 `PredictionCenter` 上监听：
  - `@version-change="handleRockVersionChange"`
  - `@segment-select="handleRockSegmentSelect"`
- 引入：
  - `setDesignRockGradeModelEnabled`
  - `flyToDesignRockGradeSegment`
  - `DesignRockGradeSegment`
- 设计版启用设计围岩分级模型。
- 超报版关闭设计围岩分级模型并恢复原隧洞模型。
- 点击区段时确保隧洞模型可见，再调用镜头跳转函数。

### 4.3 `DrawLine.ts`

主要内容：

- 新增 `DesignRockGradeSegment` 类型。
- 从 `/data/tunnel/design-rock-grades.json` 异步加载设计分级数据。
- 按 `modelIndex` 给现有隧洞 GLB Primitive 设置颜色。
- 为各区段生成里程与围岩等级标签。
- 新增 `setDesignRockGradeModelEnabled(enabled, viewer)`：
  - `true`：应用设计等级颜色和标签。
  - `false`：移除标签、轮廓并恢复原模型颜色。
- 新增 `flyToDesignRockGradeSegment(modelIndex, viewer)`：
  - 根据现有 `SEGMENT_CONFIGS` 和 `TUNNEL_END_CONFIG` 计算区段起点、终点和中心点。
  - 镜头飞行到区段中心附近。
  - 当前模型使用白色轮廓高亮，其他区段清除轮廓。

## 5. 推荐合并顺序

1. 新增 `public/data/tunnel/design-rock-grades.json`。
2. 合并 `DrawLine.ts` 中的类型、数据加载、着色、标签、恢复和飞行定位函数。
3. 合并 `PredictionCenter.vue` 的面板结构、事件和样式。
4. 合并 `index.vue` 的事件监听、导入和处理函数。
5. 启动项目进行第 6 节验收。

## 6. 验收清单

进入系统后依次检查：

1. 点击顶部“隧洞围岩”。
2. 右侧出现“围岩分级模型”面板。
3. 默认选中“设计版”。
4. 当前 17 个隧洞模型区段按Ⅱ、Ⅲ、Ⅳ、Ⅴ级显示不同颜色。
5. 模型上可见对应里程和围岩等级标签。
6. 右侧“设计分级区段”显示 17 段。
7. 点击 `YK1+116～YK1+616`，镜头跳转至近端区段，该行高亮。
8. 点击 `YK8+616～YK9+116`，镜头跳转至远端区段，新行高亮、原行取消高亮。
9. 点击“超报版”，设计版标签、颜色和轮廓被清除，原隧洞模型恢复。
10. 再切回“设计版”，分级颜色和标签重新显示。

## 7. 已完成验证

- 已在本地实际页面验证设计版加载。
- 已验证 17 个区段列表正常显示。
- 已验证近端 `YK1+116～YK1+616` 点击跳转。
- 已验证远端 `YK8+616～YK9+116` 点击跳转。
- 已验证区段选中状态互斥切换。
- 已验证切换超报版后恢复原隧洞模型。
- 针对本次修改文件执行 TypeScript 检查，未发现本次新增代码引起的类型错误。

项目当前仍存在一条与本次功能无关的历史类型提示：`src/views/index.vue` 模板中使用的 `activeAction` 未在组件类型上声明。负责人汇总时可按主项目现状统一处理。

## 8. 汇总注意事项

- 不要改变 `modelIndex` 与现有 17 个 GLB 模型加载顺序的对应关系。
- 如果主项目调整过 `SEGMENT_CONFIGS`、`TUNNEL_END_CONFIG` 或 GLB 分段数量，需要同步更新 JSON 数据。
- 如果系统部署在非根路径，但 Vite 已正确处理 `public` 目录，`/data/tunnel/design-rock-grades.json` 可正常访问；若部署配置改变了公共资源基础路径，需要同步调整数据请求地址。
- 超报版目前完成界面、切换及恢复机制，尚未接入实际超前地质预报接口和分析算法。
- 参考图片仅用于核对，不需要复制到正式项目的 `src` 或 `public` 目录。

