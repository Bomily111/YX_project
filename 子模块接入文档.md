# 钻爆法隧洞群施工数字孪生系统 — 功能架构与接入指南

## 1. 项目概述

这是一个面向钻爆法隧道施工的数字孪生平台，基于 CesiumJS 三维地理空间引擎，覆盖隧洞围岩、开挖爆破、围岩支护、通风除尘、装备调度五大施工场景，集成超前地质预报（TSP、地质雷达、瞬变电磁、超前水平钻、加深炮孔）与不良地质识别能力。

### 技术栈

| 层 | 技术 |
|---|---|
| 前端框架 | Vue 3（Composition API + `<script setup lang="ts">`） |
| 3D 引擎 | CesiumJS 1.140 |
| 构建工具 | Vite 7 |
| 状态管理 | Pinia |
| 路由 | Vue Router 4（History 模式） |
| UI 组件 | Element Plus |
| 后端 | Node.js + Express |
| 数据库 | PostgreSQL + PostGIS |
| Cesium 接入 | `vite-plugin-cesium` + `vite-plugin-externals`（映射到 `window.Cesium`） |

### 安装与启动

```bash
npm install
npm run dev          # 前端 → localhost:5173
cd backend && npm start  # 后端 → localhost:3000
```

---

## 2. 项目目录结构

```
d:\project\
├── index.html                  # 入口 HTML（加载 Cesium.js）
├── vite.config.ts              # Vite 配置 + Cesium 注入
├── package.json
├── public\data\                # 静态数据（模型、纹理、配置文件）
│   ├── tunnel\tunnel000.glb ~ tunnel016.glb   # 17 段隧道模型
│   ├── wind\suidao.glb                        # 通风模型
│   ├── blast\blast_effect.glb                 # 爆破效果（炮孔+点云）
│   ├── tem_new\tem_volume.raw + tem_model.json # 瞬变电磁体数据
│   ├── tsp_new\                                # TSP 体数据
│   ├── gpr\、ahd\、zhihu\                       # 其他地质模型数据
│   └── ...
├── src\
│   ├── main.ts                 # 应用入口（挂载 Pinia/Element/Router）
│   ├── App.vue                 # 根组件（仅含 <router-view />）
│   ├── router\index.ts         # 路由配置
│   ├── config\AppConfig.ts     # 全局配置（API 地址、Token 等）
│   ├── views\                  # 页面级视图
│   │   ├── index.vue           # 主仪表盘（核心编排页面）
│   │   ├── BlastTwinPage.vue   # 开挖爆破孪生子模块路由包装
│   │   ├── tunnel-module\      # 爆破孪生模块源码（独立 Cesium Viewer）
│   │   ├── Overview\           # 顶部导航 HUD + 场景面板
│   │   ├── admin\              # 管理后台
│   │   ├── chaobao\            # 自动化处理工作流
│   │   └── _legacy\            # 旧版代码备份
│   ├── components\             # 可复用组件
│   │   ├── DTGlobe\DTGlobe.vue # 主 Cesium 地球
│   │   ├── SceneManagement\    # 各场景/地质模型面板组件
│   │   │   ├── TEMComponents\     # 瞬变电磁
│   │   │   ├── TSPComponents\     # TSP 反演
│   │   │   ├── BLDZComponents\    # 不良地质
│   │   │   ├── AHDComponents\     # 超前水平钻
│   │   │   └── ...
│   │   └── Toolbar.vue / RoamingToolbar.vue / ...
│   ├── stores\                 # Pinia 状态管理
│   │   ├── sceneStore.ts       # 场景状态
│   │   ├── tunnelStore.ts      # 隧道/工点数据
│   │   ├── modelStore.ts       # 地质模型实例
│   │   └── monitorStore.ts     # 监控数据
│   ├── services\api\client.ts  # REST API 客户端
│   └── utils\Common\           # 共享工具
│       ├── DrawLine.ts         # 隧道模型加载/透视/场景环境
│       ├── GeoModelController.ts # 地质模型激活/定位/渲染
│       ├── Viewer.ts           # Cesium Viewer 全局引用（DTScopeEngine）
│       └── ...
├── backend\
│   └── src\
│       ├── server.js           # Express 入口
│       ├── routes\             # API 路由
│       │   ├── tunnels.js      # 隧道/工点/模型
│       │   ├── blast.js        # 爆破设计 CRUD
│       │   ├── jobs.js         # 处理任务 CRUD
│       │   └── ...
│       └── db\                 # 数据库
│           ├── 001_schema.sql  # 完整表结构
│           └── 002_seed.sql    # 种子数据
└── 隧道爆破孪生模块\            # 子模块独立项目（外部导入）
```

---

## 3. 三种模块接入模式

### 模式 A：独立路由模块（全屏接管，自建 Cesium Viewer）

**适用场景：** 需要关闭地球、自定义场景环境、或对性能有极端要求的版块。例如：开挖爆破孪生。

**做法：**

1. 将子模块源码放在独立目录，如 `src/views/your-module/`
2. 创建路由包装组件 `src/views/YourModulePage.vue`：
```vue
<template>
  <div class="wrapper">
    <div v-if="!ready" class="loading-overlay">加载中…</div>
    <YourModule v-show="ready" />
    <button class="back-btn" @click="$router.push('/')">← 返回平台</button>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import YourModule from '@/../你的子模块目录/src/YourModule.vue'

const ready = ref(false)
onMounted(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => { ready.value = true })
  })
})
</script>
```

> 双 `requestAnimationFrame` 确保 DOM 布局完成后才创建 Cesium Viewer，避免 canvas 尺寸为 0。

3. 在 `src/router/index.ts` 注册路由：
```ts
import YourModulePage from '@/views/YourModulePage.vue'
// routes 中：
{ path: '/your-module', name: 'YourModule', component: YourModulePage }
```

4. 从主界面跳转：`router.push('/your-module')`

**子模块要求：**
- 自建 Cesium Viewer（`new Cesium.Viewer(container)`），onBeforeUnmount 时 `viewer.destroy()`
- 使用平台已有的 Cesium 全局变量（`import * as Cesium from 'cesium'` → `window.Cesium`）
- 静态资源放 `public/data/` 下对应子目录
- 完全自包含，不依赖主 Viewer（`DTScopeEngine`）

---

### 模式 B：场景面板模块（共用主 Viewer）

**适用场景：** 围岩、支护、通风、调度等共用主地球的场景版块。

**做法：**

1. 在 `src/views/Overview/SceneControlPanel.vue` 注册操作按钮（在 `workface` 场景的 actionGroups 中加入）：
```ts
{ key: 'your_key', label: '显示名称', icon: '⬡' }
```

2. 在 `src/views/index.vue` 中：
   - 添加组件可见性 ref：`const isYourModule = ref(false)`
   - 在 `handleLayerSelect` 中调度：`if (name === '显示名称') isYourModule.value = true`
   - 在模板中添加：`<YourComponent v-if="isYourModule" />`
   - 在 `handleBackToOverview` 中关闭：`isYourModule.value = false`

3. 如需体数据渲染，在 `src/utils/Common/GeoModelController.ts` 的 `MODEL_CONFIGS` 中注册：
```ts
your_key: {
  key: 'your_key',
  volumeUrl: 'data/your_data/volume.json',
  tunnelPos: [lon, lat, height],
  flyDest: [x, y, z],
  flyOrientation: [heading, pitch, roll],
  lookAtOffset: [x, y, z],
  cesiumConfig: { rotate: [x,y,z], translate: [x,y,z], scale: [x,y,z] },
}
```

并将 key 加入 `VOLUME_KEYS` 数组（`index.vue` 第 588 行）。

4. 创建对应的 SceneManagement 组件（参考 `src/components/SceneManagement/TEMComponents/TEM.vue`）。

---

### 模式 C：自动化处理模块（上传 → 处理 → 可视化）

**适用场景：** 需要拿到原始数据后在平台内完成处理并展示结果的模块。例如：TSP 反演、瞬变电磁、地质雷达。

**前端已有框架（不需改动）：**
- 文件上传 UI（拖拽 + 多文件 + 进度条）
- 状态轮询（每 2 秒查一次）
- 处理完成后的可视化分发

**后端需要实现两个端点：**

**POST `/api/process/{modelKey}`**
- 接收 `multipart/form-data`，字段 `files`
- 创建 `processing_jobs` 记录（`model_type_code = modelKey`）
- 异步执行处理
- 返回 `{ jobId }`

**GET `/api/process/status/:jobId`**
- 返回：
```json
{
  "status": "pending|processing|complete|error",
  "progress": 0-100,
  "message": "当前步骤",
  "log": "增量日志",
  "error": "仅 error 时有值"
}
```

**处理脚本产出：**
1. **数据文件** → 放到 `public/data/` 对应子目录（体数据、GLB 模型、纹理等）
2. **数据库记录** → `geological_model_instances` 表插入一条，字段映射见下方第 5 节

**已有基础设施：**
- `processing_jobs` 表（`backend/src/db/001_schema.sql` 第 366 行）
- `POST /api/jobs` / `PUT /api/jobs/:id`（`backend/src/routes/jobs.js`）
- 前端 `modelStore.fetchModels()` → `mergeModelConfigsFromApi()` 自动加载

---

## 4. 前端关键服务与 API

### DTScopeEngine（全局 Viewer 引用）

```ts
import { DTScopeEngine } from '@/utils/Common/Viewer'
const viewer = DTScopeEngine.viewer   // 获取主 Cesium Viewer
```

### API 客户端模式

在 `src/services/api/client.ts` 中按领域划分 API：
```ts
export const tunnelsApi = { list, get, models, modelsByType, ... }
export const blastApi = { list, get, create, update }
export const jobsApi = { list, get, create, update }
export const sceneApi = { definitions }
```

所有请求走 `request()` 函数，自动拼接 baseUrl、超时、错误处理。

### Pinia Stores

| Store | 用途 | 关键数据 |
|---|---|---|
| `tunnelStore` | 隧道/工点 | `tunnels`, `worksites`, `currentTunnelId` |
| `modelStore` | 地质模型 | `models: ModelInstance[]`, `fetchModels(tunnelId)` |
| `sceneStore` | 场景定义 | 数据库场景配置 |
| `monitorStore` | 监控数据 | 人员/设备/环境指标 |

---

## 5. 数据模型关键接口

### ModelInstance（地质模型实例）

```ts
interface ModelInstance {
  id: string
  tunnel_id: string
  model_type_code: string   // 模型类型键（tem, tsp, gpr, weak_rock 等）
  name: string
  start_dk: number          // 起始里程
  end_dk: number            // 结束里程

  // 体数据
  volume_url: string | null

  // GLB 模型
  glb_urls: { url: string; mileage: number; heightOffset?: number }[] | null

  // 空间定位
  anchor_lon: number; anchor_lat: number; anchor_height: number
  rotation_x/y/z: number
  translate_x/y/z: number
  scale_x/y/z: number
  heading_deg: number

  // 相机飞行
  fly_dest_x/y/z: number
  fly_heading: number; fly_pitch: number
  look_at_lon/lat/height: number
  look_at_offset_x/y/z: number
  skip_look_at: boolean

  // 渲染配置
  sub_type: string
  data_type: string
  render_method: string       // 'volume' | 'glb' | 'pointcloud'
}
```

### scene_definitions（场景定义）

```sql
INSERT INTO scene_definitions (key, name, icon, color, sort_order)
VALUES ('tem', '瞬变电磁', '⚡', '#00eaff', 10);
```

### geological_model_types（模型类型注册）

```sql
INSERT INTO geological_model_types (code, name, display_name, model_format, render_method, category, sort_order)
VALUES ('tem', '瞬变电磁', 'TEM Inversion', 'raw', 'volume', 'advance_prediction', 7);
```

---

## 6. 主仪表盘（index.vue）核心状态流

```
用户点击场景导航 (OverviewHUD)
  │
  ├─ 开挖爆破 → router.push('/blast-twin')  // 独立路由
  │
  └─ 其他场景 → activeScene = key
       │
       ├─ 左侧 SceneDataPanel  → 显示指标/结构树
       ├─ 右侧 SceneControlPanel → 显示操作按钮
       │    ├─ 点击主体 → handlePanelAction(action, 'view')
       │    └─ 点击🔧 → handlePanelAction(action, 'process')
       │         └─ 打开 ProcessingWorkflowSidebar
       │              ├─ 上传 → 处理 → 轮询
       │              └─ 完成 → emit('process-complete')
       │                   └─ handleLayerSelect(key)
       │                        ├─ activateGeoModel(key) // 3D 场景
       │                        └─ 挂载对应面板组件     // 2D UI
       └─ "返回总览" → handleBackToOverview()
            ├─ 重置所有面板状态
            ├─ 恢复图层可见性
            └─ 相机飞回总览视角
```

---

## 7. Cesium 接入说明

### 主平台的 Cesium 加载方式

`index.html` 通过 `<script>` 标签加载 Cesium.js，`vite-plugin-externals` 将所有 `import ... from 'cesium'` 映射到 `window.Cesium`。**子模块不需要关心 Cesium 加载，直接用就行。**

### 隧道模型管理

| 函数 | 文件 | 说明 |
|---|---|---|
| `loadTunnelGlb(viewer?)` | `DrawLine.ts` | 加载 17 段隧道 GLB（`tunnel000~016.glb`） |
| `setTunnelGlbVisible(show)` | `DrawLine.ts` | 显示/隐藏隧道 |
| `setTunnelTranslucent(on, viewer?)` | `DrawLine.ts` | 隧道透视效果（`#00eaff` 30% alpha + REPLACE 混合） |
| `setWindTunnelTranslucent(on, viewer?)` | `DrawLine.ts` | 通风模型透视 |
| `loadWindTunnelGlb(viewer?, visible, translucent)` | `DrawLine.ts` | 加载通风模型 |

### 地球环境控制

| 函数 | 说明 |
|---|---|
| `enableBlackModelMode(viewer?)` | 隐藏地球/影像，黑色背景 |
| `restoreEarthMode(viewer?)` | 恢复地球/影像 |
| `enableTerrainTransparency(viewer?)` | 地形半透明（透视地下隧道） |

### 地质模型渲染

```ts
import { activateGeoModel, deactivateGeoModel } from '@/utils/Common/GeoModelController'
activateGeoModel('tem')   // 在场景中渲染瞬变电磁体数据并飞过去
deactivateGeoModel()      // 移除体数据
```

---

## 8. 隧道透视特效接入

图层控制中勾选"隧道模型"后出现"👁 透视"按钮。任何组件可调用：

```ts
import { setTunnelTranslucent } from '@/utils/Common/DrawLine'
setTunnelTranslucent(true)   // 隧道变半透明青色
setTunnelTranslucent(false)  // 恢复纹理
```

隧道模型通过 `colorBlendMode: REPLACE` + `#00eaff` 30% alpha 实现，不创建额外叠加层。

---

## 9. 子模块开发 Checklist

接入前确认以下事项：

- [ ] 确定接入模式（A/B/C）
- [ ] 静态资源放 `public/data/xxx/`
- [ ] 如需体数据渲染，准备 `xxx_volume.raw` + `xxx_model.json`（含 Cesium 变换元数据）
- [ ] 在 `geological_model_types` 表注册类型
- [ ] 在 `modelStore` / `GeoModelController` 中注册配置
- [ ] 如需处理流程，实现 `POST /api/process/xxx` + `GET /api/process/status/:id`
- [ ] 处理完成后插入 `geological_model_instances` 记录
- [ ] 返回按钮连接到 `router.push('/')`
- [ ] onBeforeUnmount 中销毁 Cesium Viewer（如有自建）
